# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and build
node_modules
dist
dist-ssr
*.local

# Caches
.npm
.eslintcache
*.tsbuildinfo
.vite/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
coverage

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
Desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment (secrets)
.env
.env.*
!.env.example
!.env.import.example

# Deploy / local tooling
.vercel

# Supabase CLI local runtime (linked-project cache, start secrets).
# supabase/.gitignore also ignores these.
supabase/.temp/
supabase/.branches/

# Postgres dumps and off-repo restore files (may contain guest PII)
*.dump
/backups/

# Cursor MCP (project-specific URLs or tokens)
.cursor/mcp.json

# Guest import source files (names, phones — PII)
/data/*
!/data/README.md
guest_list.xlsx
guest_list.csv

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
